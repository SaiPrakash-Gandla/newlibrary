def sum(a, b)
{
  echo "the sume of ${a} and ${b} is %{a+b}"
}

def sub(a,b)
{
  echo "The subtraction of ${a} and ${b} is ${a-b}"
}

def mul(a,b)
{
  echo "The multipilication of ${a} and ${b} is ${a*b}"
}

def div(a,b)
{
  echo "The Division of ${a} and ${b} is ${a/b}"
}
